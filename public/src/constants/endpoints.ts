export const HOST = import.meta.env.VITE_BASEURL;

export const PREFIX = '/api/';

export const BASE_URL = HOST + PREFIX;

export const ENDPOINTS = {
    COUNTRIES: "countries",
    CITIES: "cities"
}
